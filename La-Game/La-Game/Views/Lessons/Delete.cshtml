@model La_Game.Models.Lesson

@{
    ViewBag.Title = "Delete";
}

@if (Model.isHidden == 1)
{
    <h2>Activate Lesson</h2>

    <h3>Do you want to reactivate this list?</h3>
}
else
{
    <h2>Remove Lesson</h2>

    <h3>Are you sure you want to remove this?</h3>
    <h4>This cannot be undone unless you contact an admin.</h4>
}
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            Lesson
        </dt>

        <dd>
            @Html.DisplayFor(model => model.lessonName)
        </dd>

        <dt>
            Description
        </dt>

        <dd>
            @Html.DisplayFor(model => model.description)
        </dd>

        <dt>
            Language
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Language.languageName)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <button type="button" class="btn btn-default" onclick="@("window.location.href='" + @Url.Action("Index", "Lessons", new { idLanguage = Model.Language_idLanguage }) + "'");">
                <span class="glyphicon glyphicon-chevron-left"></span> Back
            </button>

            @if (Model.isHidden == 1)
            {
                <button type="submit" value="Delete" class="btn btn-default">
                    <span class="glyphicon glyphicon-plus"></span> Reactivate
                </button>
            }
            else
            {
                <button type="submit" value="Delete" class="btn btn-default">
                    <span class="glyphicon glyphicon-remove"></span> Remove
                </button>
            }
        </div>
    }
</div>
