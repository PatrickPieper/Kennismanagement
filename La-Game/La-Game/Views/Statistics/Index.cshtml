@{
    ViewBag.Title = "Index";
}

<h2> Statistics Overview </h2>

<p>
    <button type="button" class="btn btn-default btn-sm" onclick="@("window.location.href='" + @Url.Action("QuestionStatistics", "Statistics") + "'");">
        <span class="glyphicon glyphicon-stats"></span> Question Statistics
    </button>
    <button type="button" class="btn btn-default btn-sm" onclick="@("window.location.href='" + @Url.Action("LessonStatistics", "Statistics") + "'");">
        <span class="glyphicon glyphicon-stats"></span> Lesson Statistics
    </button>
</p>
<p>
    <button type="button" class="btn btn-default btn-sm" onclick="@("window.location.href='" + @Url.Action("QuestionListChoice", "Statistics") + "'");">
        <span class="glyphicon glyphicon-stats"></span> Questionlist Statistics
    </button>
</p><p>
    <button type="button" class="btn btn-default btn-sm" onclick="@("window.location.href='" + @Url.Action("Dashboard", "Home") + "'");">
        <span class="glyphicon glyphicon-chevron-left"></span> Back to dashboard
    </button>
</p>
